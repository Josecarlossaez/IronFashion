<div>
  <h3>Lista de los Productos</h3>
  <p>...</p>

  {{#each listProduct}}
    <h3>
      <a href="/product/{{_id}}/details">{{name}}</a>
    </h3>
    {{#if isAdmin}}
      <p>{{cost}}</p>
    {{/if}}
  {{/each}}

</div>